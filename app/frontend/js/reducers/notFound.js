// import { PAGE1_TOGGLE_PLAY } from '../actions/notFound.js';

const initialState = {
  n: false
};

export default function notFound(state = initialState, action) {
  return state;
}

notFound.reducer = 'notFound';
